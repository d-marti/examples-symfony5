<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Examples using Symfony 5{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🐙</text></svg>">

        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
            <link rel="stylesheet" href="{{ asset('css/app.css') }}">
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>
        <header>
            <nav>
                {% set currentPath = path(app.request.attributes.get('_route'),
                       app.request.attributes.get('_route_params')) %}
                <ul>
                    {# To loop or not to loop? THAT is the question! #}
                    <li><a href="{{ path('app_homepage') }}"{% if currentPath == path('app_homepage') %} class="current"{% endif %}>Home</a></li>
                    <li><a href="{{ path('app_homepage_docs') }}"{% if currentPath == path('app_homepage_docs') %} class="current"{% endif %}>Docs</a></li>
                    <li><a href="{{ path('app_homepage_about') }}"{% if currentPath == path('app_homepage_about') %} class="current"{% endif %}>About</a></li>
                </ul>
            </nav>

            <h1>Examples using Symfony 5</h1>
        </header>

        <main>
            {% block body %}{% endblock %}
        </main>

        <footer>
            Created by D. Marti - licensed under the <a href="{{ path('app_homepage_license') }}">MIT license</a>
        </footer>
    </body>
</html>
